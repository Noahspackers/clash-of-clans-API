[build]
functions ="src/functions"
publish = "build"

[functions]
directory = "src/functions"
node_bundler = "esbuild"
external_node_modules = [
    "express",
    "cors",
    "axios",
    "serverless"
]
[[redirects]]
  from = "/*"
  to = "/"
  status = 200
